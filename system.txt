sudo apt update --- скачать обновления для пакетов и репозиториев

sudo apt upgrade --- применить обновления

sudo apt install package_name --- установить пакет с помощью apt

neofetch --- данные о система в красивом и удобном формате

lsb_release -a --- краткая инфа о системе

sudo lshw | less --- вывод кучи инфы о системе

sudo apt --fix-broken install --- Фикисит сломанные зависимости

sudo shutdown -h 5:05 --- вырубиться через конкретное время

sudo shutdown -c --- отменить запланированное выключение

sudo reboot --- перезагрузка, можно options прокинуть

clear --- очистка всего ранее написанного в bash

history --- история введенных комманд

man command_name --- manual pages, там отдельный док команд... 
(
	/ --- поиск слова
	n --- вперед
	shift + n --- назад
)

echo $HISTSIZE --- енв переменная, показывающая сколько комманд хранится в истории

PATH=/usr/bin/custom:$PATH --- способ добавления путей к скриптам к PATH переменной

date --- сегодняшняя дата

cal --- Календарь на этот месяц

xset r rate 300 50 --- increase speed of keymaps

#######################################################################################

FUNN:

screenfetch --- все данные о системе, но neofetch круче :)

lolcat file_name -a --- вывести радужное содержимое файла

sl --- поезд

asciiquarium

fortune | -s |--- выводит какой-то рандом

cmatrix --- wake up, Neo

espeak "Hello my friend" --- произносит текст

oneko --- кошечка

aafire --- огонь

echo "How was the video? Please hit the Like button & Comment below for your feedback. Subscribe for more Awesome upcoming content." | pv -qL 10 --- Текст печатается

#######################################################################################

РАБОТА С ФАЙЛАМИ:

cd --- переход между директириями

ls --- список всего что есть в директории

ls -ltrah --- вывод красивого списка с кучей инфы (все возможные options)

cat file_name --- вывести содержимое файла

file file_name --- вывод информации о файле

locate -i file_name --- показывает все пути, где есть это название

which file_name --- показывает где файл

sudo apt list --installed | grep -i openjdk --- список всех установленных программ с 							поиском с регуляркой со словом openjdk

touch file_name.extencion --- Создать файл

sudo [degit|nano|vim] /etc/apt/sources.list --- открыть какой-то text editor 

#######################################################################################

ПРОЦЕССЫ:

top --- краткая инфа, все процессы, PID, однотонный

htop --- процессы, цвета, визуалы, удобно видеть нагрузку ядер в %, неудобный способ просмотра PID

gtop --- самыйудобный, с визуалами и графиками

#######################################################################################

РОЛИ:

adduser --- добавление пользователя с подсказками

useradd --- добавление юзера без подсказок и с опциями и агрументами

ВИДЫ РОЛЕЙ

admin --- может все
sudo --- может все

passwd --- смена пароля

#######################################################################################

СОЧЕТАНИЯ КЛАВИШ:

alt + . --- можно бегать через ранее заюзанные аргументы у конкретной команды

ctrl + shift + C --- to exit from deffernt staff(like running server)

ctrl + shift + Q --- to exit from everything in terminal

ctrl + W --- стереть слово до пробела

crtl + U --- очистить конкретную строку с текстом в BASH спереди

crtl + K --- очистить конкретную строку с текстом в BASH сзади

ctrl + L --- почему-то то же самое что и команда clear

ctrl + X + E --- перейти в редактор NANO

ctrl + enter --- разрыв страницы

shift + enter --- перенос без вставки отступа снизу

ctrl + shift + space --- неразрывный пробел

#######################################################################################

ВСЯКИЕ ЗАМЕТКИ:

- Редактирование доков:
	нельзя курсив в заголовках
	ctrl + enter --- разрыв страницы
	shift + enter --- перенос без вставки отступа снизу
	ctrl + shift + space --- неразрывный пробел
	Пример:
	    Документация по Django [электронный источник] - https://djbook.ru/

- PATH variable. It contains a list that defines which directories the shell looks in to find commands.

- APT =  Advanced Packaging Tools

- можно юзать apt вместо apt-get и apt-cashe

